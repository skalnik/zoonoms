<h1>Listing noms</h1>

<table>
  <tr>
    <th>User</th>
    <th>Animal</th>
    <th>Location</th>
    <th>Comment</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @noms.each do |nom| %>
  <tr>
    <td><%= nom.user_id %></td>
    <td><%= nom.animal_id %></td>
    <td><%= nom.location %></td>
    <td><%= nom.comment %></td>
    <td><%= link_to 'Show', nom %></td>
    <td><%= link_to 'Edit', edit_nom_path(nom) %></td>
    <td><%= link_to 'Destroy', nom, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Nom', new_nom_path %>
